<%= @user.name %>, <%= @user.email %></br>
cr_a: <%= @user.created_at %></br>
up_a: <%= @user.updated_at %></br>
T.now: <%= Time.now %>

<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <%= @user.name %>
      </h1>
    </section>
  </aside>
</div>
